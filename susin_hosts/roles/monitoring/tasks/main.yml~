---
# This playbook contains common plays to construct host, hostgroup and services for clients from the inventory and groups.  
# https://docs.ansible.com/ansible/latest/reference_appendices/special_variables.html

- name: Print all groups and hosts
  ansible.builtin.debug:
    msg:
      - " Trying to get the groups: {{ groups }} "

- name: Print all group names
  ansible.builtin.debug:
    msg:
      - " Trying to get the group names: {{ group_names | regex_replace('hosts_') }} "


  #: name="{{ yum_pkgs_host_virt }}" state=present update_cache=yes
#  when: ansible_virtualization_role == "host" and ansible_virtualization_type == "kvm" and ( ansible_distribution  == "Fedora" or ( ansible_distribution_file_variety == "RedHat" and ansible_distribution_major_version | int >= 7))
#  tags: yum_pkgs_hosts_virt
#  notify:
#    - Start libvirtd

#- name: Gather the package facts
#  package_facts:
#    manager: auto

#- name: Start the libvirtd
#  service:
#    name: libvirtd
#    state: started
#    enabled: yes
