---
# This playbook deploys the whole application stack in this site.  

    
- name: apply Ganglia config changes to all nodes
#  hosts: ganglia_owl
#  hosts: euclid_cluster
  hosts: charybdis
  remote_user: root

  roles:
    - ganglia


